services:
  db:
    image: postgres:17
    container_name: habiter-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - habiter_data:/var/lib/postgresql/data

volumes:
  habiter_data: